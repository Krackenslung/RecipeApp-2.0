# Client — inlined into the bundle at build time. Public by design.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server only. NEVER prefix these with VITE_.
GEMINI_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
